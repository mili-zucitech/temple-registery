package com.templeregistry.service.workflow;

import com.templeregistry.entity.workflow.WorkflowAction;
import com.templeregistry.entity.workflow.WorkflowStatus;

import java.util.List;
import java.util.Optional;
import java.util.stream.Collectors;

/**
 * Patch: Add findAll() method to TransitionRuleRegistry (needed by WorkflowEngineImpl.computeAvailableActions).
 * Already in TransitionRuleRegistry — this note is a reminder to add the method.
 * The method is added directly to TransitionRuleRegistry.java.
 */
// THIS FILE IS INTENTIONALLY EMPTY — the findAll() method is in TransitionRuleRegistry
// Delete this file after verifying TransitionRuleRegistry has findAll()
public class _Placeholder { }
